[project]
name = "small-libspec-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "libspec",
]

[tool.uv.sources]
libspec = { git = "https://github.com/drhodes/libspec" }
